{% extends 'base.html.twig' %}

{% block title %}Planification{% endblock %}

{% block body %}
    <h1>Planification</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ planification.id }}</td>
            </tr>
            <tr>
                <th>HeureDebut</th>
                <td>{{ planification.HeureDebut }}</td>
            </tr>
            <tr>
                <th>HeureFin</th>
                <td>{{ planification.HeureFin }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ planification.date ? planification.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_planification_index') }}">back to list</a>

    <a href="{{ path('app_planification_edit', {'id': planification.id}) }}">edit</a>

    {{ include('planification/_delete_form.html.twig') }}
{% endblock %}
