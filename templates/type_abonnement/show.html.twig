{% extends 'base.html.twig' %}

{% block title %}TypeAbonnement{% endblock %}

{% block body %}
    <h1>TypeAbonnement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_abonnement.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ type_abonnement.type }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ type_abonnement.prix }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_abonnement.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_abonnement_index') }}">back to list</a>

    <a href="{{ path('app_type_abonnement_edit', {'id': type_abonnement.id}) }}">edit</a>

    {{ include('type_abonnement/_delete_form.html.twig') }}
{% endblock %}
